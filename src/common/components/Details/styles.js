import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 0.9, 
        alignItems: 'center',
      },
    textinput: {
      borderWidth: 1, 
      borderRadius: 4, 
      width: '92%', 
      height:'50%', 
      textAlign: 'center',
    },
});

export {styles};